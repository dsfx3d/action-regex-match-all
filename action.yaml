name: Regex Match All
description: Outputs the CSV of all unique matches or an empty list

inputs:
  regex:
    required: true
    description: the regex to match
  input:
    required: true
    description: the string to match against

runs:
  using: node16
  main: index.js
